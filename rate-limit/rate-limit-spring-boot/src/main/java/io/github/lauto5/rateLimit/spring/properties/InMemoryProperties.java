package io.github.lauto5.rateLimit.spring.properties;

/**
 * Configuration for the in-memory persistence adapter.
 *
 * <p>Kept as a typed placeholder: the in-memory store currently has no tunable options. Future
 * options (for example a maximum entry count or a capacity) apply only to this adapter, never
 * as a flat global block.
 */
public class InMemoryProperties {
}